const { game_of_life } = require("./gameoflife");

it("abc", () => {
  expect(1).toEqual(1);
});

describe("count neighbours", () => {
  it("empty", () => {
    //game_of_life.count_neighbours
  });
});
